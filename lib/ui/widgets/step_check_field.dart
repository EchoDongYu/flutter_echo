import 'package:flutter/material.dart';

class StepCheckField extends StatelessWidget {
  const StepCheckField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
